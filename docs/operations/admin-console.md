# M-Agent 本机管理后台

本文档只说明管理后台当前已经具备的能力、运行方法和安全边界。未完成的后台功能统一记录在 `docs/development/TODO.md`。

## 定位

管理后台是仅供本机使用的项目观察和配置工具，不是面向企业微信用户的业务入口，也不是多人管理平台。

默认监听：

```text
127.0.0.1:8787
```

不要直接暴露到公网或公司内网。

## 启动

```bash
uv run --locked python -m app.admin.server --port 8787
```

浏览器访问：

```text
http://127.0.0.1:8787/
```

## 当前功能

### 项目总览

首页把“结构”和“状态”拆开，避免把不同性质的内容塞进同一套层级。

上半部分是分层展开的受控 Agent 运行架构：

```text
业务入口
  -> 受控 Agent 内核：接入与权限 -> 任务编排与上下文 -> Agent 运行与授权
  -> 业务处理：写作域 / 审核域
  -> 结果与附件交付

贯穿约束：身份隔离 / 工具白名单 / 结构化输出 / 幂等恢复 / 交付确认
共享执行底座：文档解析 / 网页读取与搜索 / 政策库 / 微众银行信息库
管理与治理面：管理台 / 可观测性 / 运行数据 / 研发交付 / 知识治理
```

写作和审核使用不同颜色，并在各自区域内展开已经存在的核心能力：

- 写作：直报、单素材简报、多素材简报、续写改稿、专题内容生产。
- 审核：通用审核、专项审核、格式审核、多文件联合审核。

更细的综合调研、深银协动态、内参周报，以及通用文字、通用 Word、HTML、PPTX、内参、半月报等能力位于下半部分“功能与模块状态”，按七个职责组逐项展示。状态筛选只作用于这份明细，不会把架构图节点隐藏掉。

主图使用“主链 + 固定分区”：动态线只连接业务入口、三层 Agent 内核、写作/审核域和结果交付，直接路径没有节点阻挡时使用直线箭头，必须避让时才转为圆角直角路径；子能力、工具、知识和治理控制按对齐分组表达，不把每个依赖都画成跨区连线。五道运行约束明确显示安全与可靠性不是只靠 Prompt。全部架构节点均可查看职责和代码依据，窄屏时仅在图内横向浏览。图由服务端 HTML、CSS 和原生 SVG 生成，不依赖第三方关系图库或 CDN；用户暂停动效或系统启用“减少动态效果”时，动态线停止。

七个明细组的边界是：

- `业务入口`：只放业务用户提交请求的入口，不放运维 Bot 和管理台。
- `智能体底座`：接入权限、路由注册、Agent 运行、多任务关系与版本、任务组装、持久执行和任务状态存储。
- `业务能力`：用户可直接获得结果的写作和审核能力。
- `领域公共组件`：只被业务域内部复用、不能由用户直接选择的公共处理组件。
- `共享工具服务`：文档解析、网页读取与搜索、结果与附件交付等受限动作能力。
- `知识资产`：政策库、微众银行信息库及其内容治理入口。
- `管理与治理`：管理台、运维可观测性、运行数据治理和研发交付保障。

这里的逻辑分类不强制等同于 Python 目录。例如文档服务和运维代码目前都位于 `app/platform/` 下，但在项目总览中分别属于共享工具服务和管理与治理；不能因为物理目录相同就把它们都解释为智能体底座。

### 状态口径

管理后台不调用大模型推断建设状态，也不维护第二套项目进度。页面状态只组合以下可核对事实：

- `skills/*/config.yaml` 的启用状态。
- `docs/development/TODO.md` 中仍开放事项的状态和下一动作。
- 对应代码模块是否存在。
- 写作、审核、材料润色和运维 Bot 的心跳。

建设状态包括：

- `稳定运行`：当前能力已作为稳定基础使用。
- `已上线·优化中`：主体能力可用，但仍有开放的质量或体验待办。
- `建设中`：关联待办正在进行。
- `待建设`：关联待办未开始，且没有已实施证据。
- `已暂缓`：关联待办明确暂停。
- `已关闭`：Skill 已安装但当前关闭。

建设成熟度和运行在线状态互相独立。例如“已上线·优化中 / 心跳超时”表示功能已经存在，但当前 Bot 进程需要处理。

业务能力节点还显示“持久队列”或“实时执行”。该标记直接读取写作和审核 handler 的真实注册表：深银协动态与八类审核显示为持久队列；综合调研、内参周报和即时润色显示为实时执行。迁移代码与管理台标记因此使用同一事实来源，不需要另维护一份清单。

架构节点、明确关系、状态清单组件、职责和代码证据都维护在 `app/admin/services.py`。主图呈现全部登记架构节点，但只把关键关系画成主干信息流；新增、删除或改名时，必须按所属颗粒度同步对应表和测试，不能用状态清单替代架构图，也不能把架构图当作逐个实现文件的完整清单。

“任务关系与多任务卡片”在智能体底座组中独立展示为稳定能力，表示写作和润色入口已经可以定位多篇稿件并恢复追问；这不代表审核入口的任务追加和切换已经完成。

### 板块信息

管理后台展示底座、写作、审核、知识库、入口运维和管理后台六个板块的当前事实、最近更新和首要开放待办。政策库和银行信息库当前只展示汇总数量，不展示材料正文。首页不再单设审核子能力统计表；审核任务数据仍由后台按能力归集，供运行诊断和后续专项分析使用。

### Skill 开关

后台读取并修改：

```text
skills/<skill_id>/config.yaml
```

只允许修改 `enabled: true/false`。开启 Skill 只表示它进入路由候选；实际执行仍要求 workflow 可用，且用户权限允许。

### 用户权限和任务记录

用户权限和最近任务记录默认不读取、不展示，也不会进入默认页面源码。管理员主动打开以下地址后才加载：

```text
/?show_sensitive=1#users
```

用户权限来自本机文件：

```text
config/platform-policy.yaml
```

最近任务来自统一非 Git 数据根目录，只展示必要摘要，不展示用户原文、附件内容、模型提示词或密钥。写作和审核板块保留任务数量汇总，但只读取状态及元信息文件。

### Bot 心跳

页面读取四个 Bot 的心跳用于本机观察：

- 写作 Bot
- 审核 Bot
- 材料润色 Bot
- 运维 Bot

控制台能显示某个服务，不代表运维 Bot 已对该服务配置掉线告警；告警范围仍以运维配置为准。

## 安全边界

- 默认只绑定本机地址。
- 不读取或展示 `.env`、API Key、Bot Secret。
- 不提供任意文件浏览、任意 SQL 或任意命令执行。
- Git 查询是代码内固定的只读命令，不接受页面传入命令，也不自动联网。
- 页面只允许写 Skill 开关和用户权限；项目总览、TODO、运行状态和更新记录均为只读。
- 动态文字必须转义，避免 Skill、TODO 或 Git 摘要被当作 HTML 执行。

如果以后改成多人后台，必须先完成登录鉴权、操作审计、CSRF 防护、细粒度权限和内网部署方案。这些需求在实施前只进入 TODO，不写入本文的当前能力。

## 代码边界

```text
app/admin/                         # 后台实现
tests/test_admin_services.py       # 数据服务测试
tests/test_admin_server.py         # 页面和接口测试
docs/operations/admin-console.md   # 当前运行与操作说明
```

管理后台只负责配置和观察，不能直接实现写作、审核或知识库业务逻辑。

## 验证

```bash
uv run --locked pytest tests/test_admin_services.py tests/test_admin_server.py -v
uv run --locked python scripts/project_docs.py check
```

测试至少覆盖：

- 业务运行面、管理与治理面及其关系引用完整。
- 架构图展示全部登记节点，但只为七条主干关系绘线；子能力、工具、知识和治理固定分区，状态清单继续保留建设状态颗粒度。
- 仓库内每个已安装 Skill 都映射到状态清单组件。
- TODO、Skill 状态、代码证据和心跳能形成正确状态。
- 页面使用原生 SVG 生成信息流，不依赖第三方关系图库或 CDN。
- Bot 心跳缺失、超时或损坏时安全降级。
- 默认页面不调用用户权限和任务读取函数。
- 动态文字被正确转义。
- 页面不泄露密钥或用户材料正文。
- 审核子能力统计不进入默认首页，底层任务类型回退归类、处理/交付状态和平均耗时口径仍有服务层测试保护。

政策库可视化管理等尚未实现的后台能力见 `docs/development/TODO.md`，不能根据本文或页面占位说明为已完成。
