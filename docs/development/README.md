# M-Agent 后续开发总指南

本文档给 Codex、Claude Code 和后续开发者使用。目标是让后续开发可以通过自然语言持续推进，同时不破坏现有功能。

## 一句话结论

M-Agent 后续采用：

```text
M-Agent 自建底座
  + Pydantic AI 运行层
  + 文件化 skills
  + 受限工具层
  + 企业微信统一入口
```

它不是 Dify 这样的可视化平台，也不是 Hermes/OpenClaw 那种个人助手系统。它是一个代码型、可测试、适合 Codex/Claude Code 持续开发的业务 agent 底座。

## 当前重点

已经跑通的最小链路：

```text
本地 CLI / 本地 demo 输入自然语言
  -> 路由到 direct_report
  -> 创建独立任务目录
  -> 校验用户是否有 skill 权限
  -> 读取用户 URL
  -> Pydantic AI Agent 生成结构化结果
  -> 写入任务结果
  -> 输出标题、正文、来源
```

底座当前还具备：

- 直报 Bot 真实企业微信文本入口适配。
- 企业微信文本消息核心适配。
- 写作 Bot 短任务组装 v1：支持用户先发材料再说明用途，或先说明用途再连续发送多个材料，最后回复“开始写”；结构化任务需要澄清时保留原文件和会话，用户回答后自动续跑。
- 写作 Bot 文件消息接收 v1：可把企业微信 Word/PDF/PPTX 持久化暂存为结构化请求材料，Bot 重启后可恢复有效会话；后续仍需把组装接口下沉为底座公共能力。
- 网页读取，以及 DOCX/PDF/PPTX 统一文档读取工具；完整解析结果保存在任务 `work/`，扫描 PDF 可按需 OCR，PDF/PPTX 可显式逐页渲染。
- 用户/skill 权限策略。
- 共享用户名称映射表，写作 Bot 和审核 Bot 都能把企业微信 `userid` 对应到可读用户名。
- 每次请求独立 job 目录。
- 平台级会话状态和稿件版本链 v1。
- 开发期写作对话日志，可用于前期问题排查，稳定后可关闭。
- CLI 配置检查。
- 本机项目控制台：保留 Skill 开关、用户权限和最近任务，通过可缩放关系图和状态清单自动展示五层整体架构、功能关系、建设状态、六个板块进展、开放 TODO、最近 Git 更新、知识库/任务数量和 Bot 心跳。

后续重点：

1. 立即推进 `TODO-027` 的生产接入：先让直报写作进入持久化任务执行器，完成快速受理、重复消息、排队、取消、进程重启和附件发送幂等验收。
2. 直报试点稳定后，再按 `writer1`、`writer2`、`research_synthesis` 的顺序切换写作重任务；审核 Bot 继续保持独立，不在第一批切流。
3. `TODO-023` 已完成首批 5 份真实文件运行和首轮人工评分：46 条候选中 34 条有效、11 条误报、1 条未评分，46 条标注位置均评为准确。审核质量优化继续独立推进，但不替代当前底座生产接入。
4. `TODO-021` 已实现 `.docx` 多文件联合审核 v1；公共任务协议和附件交付已下沉，后续用真实正文和附件完成质量与回传验收。
5. 扫描 PDF 按需 OCR、PDF/PPT 页面渲染和公共大文件回传已具备；推进 PPT 专项审核时仍需单独建设视觉规则和标注，不能把“能渲染 PPT”误当成“已经能审核 PPT”。
6. 直报、简报和综合调研的内容质量继续由真实样本驱动优化，不在持久任务切流期间同步大规模重写业务工作流。

## 当前进度快照

更新时间：2026-07-15

### 本轮 P1 底座交付的实际含义

2026-07-15 完成的是一套供所有 Skill 复用的生产底盘，不是新增某一个写作功能：

- **收材料**：写作和审核共用安全、可恢复的任务暂存内核，并统一 `wait`、`submit`、`cancel`、`bypass` 协议。
- **跑任务**：持久化任务执行器内核已具备消息幂等、排队、并发限制、取消、租约和进程重启恢复能力。
- **读文件**：扫描 PDF 可按需 OCR，PDF/PPTX 可在有需要时逐页渲染，并设置页数、时间、像素和容量上限。
- **发结果**：写作和审核共用附件交付能力，支持动态超时、完整重试、运维告警和任务编号兜底，不静默压缩图片。

必须区分当前生效范围：公共暂存、文档读取和附件交付已经接入现有入口；持久化执行器已经完成并通过自动化测试，但现有写作、审核重任务尚未全部切换。因此，“底座能恢复任务”不等于“当前所有 Bot 重启后都能自动续跑”。下一步先用直报做生产接入试点，再逐项扩大范围。

当前整体状态：

- 审核 Bot 继续独立运行，已支持内参周报、半月报、通用 Word/文字审核、显式公文格式审核和 `.docx` 多文件联合审核 v1，不急于迁移到统一入口。
- 通用审核真实质量评测工具已落地：历史文件按文件内容和正文近重复双重去重，首批 5 份覆盖问卷、附件引用、超长文、表格和常规材料；真实结果留在 `M-Agent-Files/evaluations/`，仓库只保留工具、测试和脱敏说明。
- 首轮人工评分确认 46 条候选的自动标注位置全部准确；英文 `People’s`、`China’s`、`people‘s` 等弯撇号被 `quote-pair` 误当中文单引号的问题已修复，真正缺失的中文引号仍会保留。
- 首轮剩余误报已采用离线定向回放修复：明确替换意见增加原文证据闸门，短英文标签不再按正文残缺处理；原始 Word 中真实存在的 `、 ` 不删除意见，而是精确标记并说明为顿号后多余空格。
- 格式审核支持两种消息顺序：先说格式审核再发文件，或先发文件后在 30 分钟内补说格式审核；待处理状态持久化，Bot 重启后可恢复。
- 用户直接发 1 份 `.docx` 时自动走原单文件审核，连续发 2 至 5 份时无需“联合审核”或“开始审核”指令，自动归为联合审核；文件到达即确认，后台默认使用 8 秒静默窗口分流。
- 联合审核不按上传顺序默认正文；系统综合文件名和正文附件引用识别主文件，证据仍不唯一时才要求用户指定，再执行逐文件审核、附件确定性检查和双边原文证据的跨文件语义检查。
- 通用审核已具备代码规则、分段语义审核、`200` 至 `100000` 字通篇逻辑校对、长文双扫描候选复核和内容匹配优先的 Word 标注流程。
- 独立公文格式审核已用桌面真实模板验证零问题；多文件联合审核 v1 已有专项自动化覆盖，当前缺口是经授权真实文件内容审核回归集、真实多文件企业微信验收，以及 PDF/Excel/PPT 联合审核和 PPT 专项审核。
- 直报 Bot 已接入新底座，真实企业微信文本链路已经验证。
- DeepSeek 写作底座当前实际使用 OpenAI 兼容 `/v1` 通道，推荐使用 `MODEL_*` 配置；审核 Bot 的 DeepSeek 配置仍是独立的 Anthropic 兼容通道。
- 已修复模型输出上限硬编码 `2048` 导致直报“处理失败”的问题，默认输出上限为 `4096`。
- 已修复 `deepseek-v4-flash` thinking mode 与结构化输出不兼容的问题。
- 直报 critic 默认改为 `advisory` 模式：做语义质检并提示风险，但不因 critic 自动重写，避免质检层增加真实 Bot 失败概率。
- 直报生成和“精简一下”等上一稿改稿已通过真实链路验证。
- 已新增平台级 `ConversationStore` 和 `intent` 意图分类器，直报和简报改稿共用同一套活跃稿件、版本链和改稿/新任务判断。
- 已增强改稿执行协议：上一稿材料不再被 2000 字过早截断，且会对“只改标题”“不要拆段”“改变原文意思”等要求加入局部编辑约束。
- 已新增独立 `rewrite` skill v1：支持用户直接贴文字做材料润色，默认只优化表达、不新增事实，并可继续对话式修改。
- 已补“新润色任务”和“旧稿续改”的切割规则：用户明确贴新正文要求润色时，即使当前有活跃直报/简报会话，也会新开 `rewrite`，不会误改上一稿。
- 已新增独立 `research_synthesis` 综合调研整合 skill v1：以用户上传的现成提纲为骨架，把多部门 Word/PDF/PPTX 素材归入对应章节；明确总提纲文件名和正文答复特征共同参与识别，仍有歧义时保留原任务追问并在用户回答后续跑，材料缺口和口径冲突不静默补齐。
- 综合调研已升级为“材料台账—正文生成”两阶段：台账先按提纲问题登记综合事实、规范化来源、缺口、冲突和图片位置，正文再跨部门归并；生成后独立校正标题编号、段末来源标签、原始文件名、遗漏的提纲一级主题和连续图片提醒。
- 综合调研整合已增加 Word 交付：第一阶段保留规范化部门来源标签；DOCX 图片不做 OCR、不嵌入成稿，在对应小节保留人工评估提醒；生成稿复用审核模块现有公文基本格式，开头和结尾仅保留待补备注，并由写作 Bot 作为企业微信文件返回。
- 写作入口每次任务文件数量上限已放宽到 10 份，文件总大小仍保持 20MB；审核 Bot 的联合审核范围不随之改变。
- 已新增开发期 `ChatLogStore`，记录完整用户输入、即时提示、最终回复、意图、skill、job、用户名和版本信息。
- 已将审核 Bot 原有用户名注册表抽到 `app/platform/user_registry.py`，写作 Bot 和审核 Bot 共用外部运行数据目录中的用户名表。
- 写作 Bot 的即时提示已改为按实际 skill 动态变化：直报、单素材简报、多素材简报不再共用“直报写作流程”话术。
- 已新增独立运维 Bot：异常和链接读取失败等事件写入统一运行数据目录，运维 Bot 独立长连接发送当天实时告警，并在每个工作日 9:00 发送前一工作日日报；历史事件不在重启后逐条补发，同一天完全相同的事件只通知一次，写作 Bot 和审核 Bot 已接入心跳监控。
- 已将用户上传、系统生成、知识库、日志和运行状态统一剥离到桌面 `M-Agent-Files/`；代码仓库不再承载真实运行数据。写作和审核任务均按年/月归档，审核原件与标注结果分别存入 `input/` 和 `output/`。
- 后续 PDF、PPT 等格式的待组装文件、解析结果、渲染图片和 OCR 中间产物也继续统一存入桌面 `M-Agent-Files/`；HTML 文件支持暂缓，现有网页链接读取能力保持不变。
- 已新增底座统一文档服务 v1：DOCX、PDF、PPTX 共用路径隔离、类型校验、宏/异常压缩包拦截和标准文档结构；PDF 保留页码，PPTX 保留幻灯片号、表格、图表、备注、对象位置和图片，完整 JSON 写入任务 `work/documents/`。
- 写作 Bot 的待组装文件已持久化到 `M-Agent-Files/runtime/intake/`；有效期内即使 Bot 重启，用户仍可继续补充用途或发送“开始写”。
- 写作和审核的短任务已下沉到公共 `app/platform/intake.py`：统一匿名用户目录、原子状态、文件安全暂存、重启恢复、TTL 清理、防伪造路径、文件限制，以及材料引用、`wait/submit/cancel/bypass` 动作和结构化提交；两套业务状态机只保留各自判断规则。
- 已新增公共 `AttachmentDelivery`：写作和审核共用目录校验、串行上传、动态超时、完整重试、交付状态和运维事件，约 50MB SDK 上限外或多次失败时返回任务编号供人工取回；当前不自动压缩图片。
- 已新增 SQLite 持久化任务执行器内核：消息幂等、分级并发、租约与 fencing token、心跳、周期恢复、取消、待补充恢复和跨进程状态版本已完成。现有 Bot 重任务尚未全量切流，需按 handler 的可恢复性和外部副作用幂等逐项启用。
- 本机管理后台已升级为项目控制台，项目状态直接从代码、Git、TODO、任务目录、知识库和运维心跳汇总，不再单独维护一份手工进度清单。
- 写作和审核任务已增加不含正文的 `status.json` 状态索引；控制台把成稿、待补充、失败和中断分开统计，并兼容审核历史 `meta.md` 归档。处理完成与企业微信成功交付是两个口径，未验证交付时不会显示成已送达。
- 项目已统一到 uv 管理的 Python 3.13.14 和根目录独立 `.venv`；`pyproject.toml` 是直接依赖唯一正式声明，`uv.lock` 固定准确版本，开发、测试、Bot、Git hooks 和 AI 工具统一使用 `uv run --locked ...`。
- 项目控制台已增加五层交互式架构关系图和状态清单，可按稳定运行、上线优化、建设中、待建设、暂缓和关闭筛选；支持缩放、拖动和节点详情，建设成熟度与 Bot 当前在线状态分开显示；开源图组件已本地固化，不依赖 CDN。
- 2026-07-13 统一文档服务交付后的项目自动化测试（不含真实模型测试）共 `560` 项通过，审核 Bot 独立保护测试 `9` 项通过。
- 2026-07-14 uv 独立环境迁移后，全仓离线自动化测试（不含真实模型测试）共 `619` 项通过，审核 Bot 独立保护测试 `9` 项通过；底座、写作、审核和运维配置检查均使用 `.venv` 中的 Python 3.13.14 通过。
- 2026-07-14 任务状态统计修复后，全仓离线自动化测试（不含真实模型测试）共 `628` 项通过，审核 Bot 独立保护测试 `9` 项通过；真实模型测试因当前网络 `Connection error` 未通过，不归因于本次统计变更。
- 2026-07-14 综合调研整合 v1 完成后，全仓离线自动化测试（排除真实模型测试）共 `638` 项通过，审核 Bot 独立保护测试 `9` 项通过；另行执行全仓含真实 LLM 用例时 `666` 项通过、`1` 项因模型把正常目录项误报为正文缺失而失败，该失败位于旧审核模型质量用例，不归因于本次写作 skill 变更。
- 2026-07-14 写作入口文件数量上限调整为 10 份后，全仓离线自动化测试（排除真实模型测试）共 `639` 项通过，审核 Bot 独立保护测试 `9` 项通过；文件总大小上限仍为 20MB。
- 2026-07-14 综合调研首轮真实测试问题修复后，全仓离线自动化测试（排除真实模型测试）共 `645` 项通过，审核 Bot 独立保护测试 `9` 项通过；新增覆盖自然说法路由、多份“提纲”文件识别、待澄清文件保留、用户回答自动续跑和 Bot 重启后恢复。
- 2026-07-14 综合调研 Word 输出和图片提醒完成后，全仓离线自动化测试（排除真实模型测试）共 `651` 项通过，审核 Bot 独立保护测试 `9` 项通过；新增覆盖 DOCX 图片原位置提醒、长材料提醒保留、部门标签、公文基本格式 Word、无图片嵌入、首尾备注、任务输出目录隔离和企业微信附件回传。
- 2026-07-14 综合调研“材料台账—正文生成”两阶段优化后，全仓离线自动化测试（排除真实模型测试）共 `654` 项通过，审核 Bot 独立保护测试 `9` 项通过；新增覆盖两阶段调用、规范化部门名、段末合并来源、中文标题编号、遗漏提纲一级主题补回和重复图片提醒计数。
- 2026-07-15 公共任务暂存内核下沉后，全仓离线自动化测试（排除真实模型测试和并行开发中尚未提交的审核质量测试）共 `661` 项通过，审核 Bot 独立保护测试 `9` 项通过；写作和审核原有状态机保持不变，新增覆盖原子状态、匿名隔离目录、路径防伪、TTL 清理和统一文件限制。
- 2026-07-15 P1 底座补齐后，全仓离线自动化测试（排除真实模型测试）共 `799` 项通过，P1 底座与入口联合回归 `307` 项通过，审核 Bot 独立保护测试 `9` 项通过；新增覆盖公共任务协议、企业微信消息幂等、持久队列、分级并发、租约与跨进程执行锁、重复取消、状态版本、公共附件交付、扫描 PDF OCR、PDF/PPTX 页面渲染和资源限制。
- 本机首次迁移已通过逐文件 SHA-256 校验；旧目录已移入 `M-Agent-Files/legacy/pre-migration-source/` 作为临时回滚备份，没有删除历史材料。
- 不可读取网页会返回安全提示，不再把底层 curl 错误直接暴露给用户。
- 简报写作已支持链接读取失败兜底：有链接读取失败时先询问用户，是继续使用已读取素材写，还是粘贴失败链接正文后再一起写。
- 已建立直报质量回归测试集 v1，固定使用 4 个用户提供的公开链接做质量对比。
- 已固化主体称谓：直报全篇只写“微众银行”；简报首次写“深圳前海微众银行（以下简称“我行”）”，后文写“我行”。
- 简报当前真实口径已明确：报送对象为深圳市金融办、南山区、前海管理局、深圳人行、深圳金监局等地方政府和监管部门，主要展示微众银行近期动态及成果；篇幅正常控制在 `1000` 字左右，最长不超过 `1200` 字。
- 已按经授权样本沉淀第一版简报类型分类和 A 类样本写法：综合成果型、机制成果型、产品工具型、平台合作型、标准引领型、能力建设型、外部认可型、活动亮相型、专项治理型。
- 已新增直报政策挂接闸门：案件、活动、获奖、直播、判决等时间节点稿件默认直入主题，不额外注入政策库材料；产品、机制、长期服务类素材才允许补充政策背景。
- 已新增共享政策研究层 v1：`direct_report`、`writer1`、`writer2` 统一通过 `policy_research` 判断“能不能挂、挂哪条、为什么、摘哪一句”，政策库已补启用状态和标签治理字段。
- 已新增写作 Bot 短任务组装层 `app/writing/intake.py`：支持“先发材料后说用途”“先说用途后发材料”“多条链接/文字/文件发完后回复开始写”，并通过 `PlatformApp.handle_structured_request(...)` 进入直报、简报、综合调研整合或润色 skill。
- 写作 Bot 已注册 `message.file` 入口，Word/PDF/PPTX 文件可作为直报、简报和综合调研整合的本次任务材料；独立 `rewrite` v1 仍只支持直接粘贴文字。当前文件会话仍是写作入口 v1，后续要下沉为底座级公共组装能力。

最近真实测试结论：

- `baijiahao.baidu.com` 测试链接可以成功生成直报。
- `www.cet.com.cn` 测试链接在本机环境下 HTTPS 返回 `SSL_ERROR_SYSCALL`，HTTP 返回 `Empty reply from server`。这属于网页读取失败，不是模型失败。

下一步建议：

1. 按 `TODO-027` 先把直报写作切换到持久化执行器，完成真实企业微信受理、重启、重复消息、取消、多用户排队和附件发送失败验收。
2. 直报稳定后依次切换 `writer1`、`writer2` 和 `research_synthesis`；每个 handler 单独声明能否安全重跑，不能一次性全量开启恢复。
3. 审核 Bot 保持独立运行，继续完成 `TODO-023` 质量基线和 `TODO-021` 多文件真实验收，暂不作为第一批执行器切流对象。
4. 上述生产接入稳定后，再推进 `TODO-020` PPT 专项审核；HTML 文件支持继续暂缓。

完整优先级、依赖顺序和并行策略统一以 `docs/development/TODO.md` 顶部“统筹视图”为准。

## 当前待办

当前待办统一维护在：

```text
docs/development/TODO.md
```

规则：

- `TODO.md` 记录当前还没完成、需要后续推进的事项。
- Git 提交和核心文档记录真实项目状态；`STATUS-REPORT.md` 仅作为本机自动开发日志，不进入 Git。
- 新增、完成、暂缓或取消待办时，必须同步更新 `TODO.md` 和相关模块文档，不以本机状态报告代替核心文档。

## 文档地图

```text
docs/
├── README.md                         # 文档分区总览
├── archive/                          # 历史方案，不作为新开发依据
├── development/
│   ├── README.md                     # 本文件
│   ├── admin-console.md              # 本机管理后台说明
│   ├── architecture.md               # 整体架构说明
│   ├── bank-knowledge-base.md        # 微众银行信息库说明
│   ├── codex-claude-workflow.md      # 用 Codex/Claude Code 开发的流程
│   ├── direct-report-production-test.md # 直报 Bot 生产级测试方案
│   ├── direct-report-quality-phase1.md # 直报质量第一阶段规则说明
│   ├── direct-report-quality-regression-v1.md # 直报质量回归测试集 v1
│   ├── direct-report-quality-review-20260707.md # 4 个直报样本人工评审记录
│   ├── directory-standard.md         # 目录和文件规范
│   ├── policy-knowledge-base.md      # 政策知识库与直报政策研究层说明
│   ├── status-report.md              # 本地状态日志和自动记录机制
│   ├── TODO.md                       # 当前待办统一入口
│   └── testing-and-delivery.md       # 测试和交付规范
├── agent-platform/
│   └── README.md                     # 底座区规划
└── capabilities/
    └── README.md                     # 功能区/skills 规划
```

## 开发前阅读顺序

通用开发：

1. `AGENTS.md` 或 `CLAUDE.md`
2. `docs/development/README.md`
3. `docs/development/architecture.md`
4. `docs/development/codex-claude-workflow.md`
5. `docs/development/TODO.md`

改底座：

1. `docs/agent-platform/README.md`
2. `docs/development/directory-standard.md`
3. 相关 `app/platform/` 文件

改 skill：

1. `docs/capabilities/README.md`
2. 对应 `skills/<skill_id>/SKILL.md`
3. 对应 `skills/<skill_id>/config.yaml`
4. 对应测试文件

## 后续自然语言开发示例

新增 skill：

```text
新增一个“简报写作”skill。
输入是网页链接或 Word/PDF/PPTX 文件。
输出包括标题、摘要、正文、来源。
请按 direct_report 的结构实现，并补测试。
```

优化 skill：

```text
优化 direct_report skill。
现在生成内容太像新闻稿，需要更像国办直报材料。
请调整 SKILL.md、prompts/draft.md 和测试。
```

新增工具：

```text
新增 pdf_reader 工具。
只能读取用户本次上传的 PDF，不能读取本机其他目录。
让 direct_report、writer1 和 writer2 可以声明使用它。
```

接企业微信：

```text
新增统一企业微信入口。
收到文本或链接后走 app/platform 的路由和 runtime。
先只开放 direct_report。
不要影响现有 app/review Bot。
```

检查新底座配置：

```text
运行 uv run --locked python -m app.platform.cli --check-config。
如果配置正常，再用一条本地消息测试 PlatformApp。
```

## 不要做的事

- 不要把所有能力写进一个大 prompt。
- 不要让模型自由决定调用任意工具。
- 不要在 skill 中直接读取 `.env`。
- 不要让外部用户访问本机任意文件。
- 不要一上来重写 `app/review/` 或 `app/writing/`。
- 不要为了跑 demo 把密钥写进代码。
